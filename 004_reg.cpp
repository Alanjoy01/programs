#include <iostream>
using namespace std;

int main(){
	int length = 10, breadth = 6;			// length and width of rectangle
	cout << "Area of rectangle with length '" << length << "' and width '" << breadth << "' is: " << length*breadth;// area of rect = l*b
	return 0;
}